version: '3'

services:
  lambda:
    build: .
    env_file:
     - ./.aws.env
    environment:
      - PYTHONPATH=/var/task/src:/var/task/lib
      - PATH=/var/task/bin
    volumes:
      - ./src/:/var/task/src/
    command: src.crawler.crawler_func